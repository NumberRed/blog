<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css">
    <link rel="stylesheet" href="/stylesheets/main.css">
</head>
    <body>
        <div class="ui fixed inverted menu">
			<div class="ui container">
				<div class="header item">
					<img src="https://i.pinimg.com/originals/18/3a/64/183a64ed166744236a286a2e69c916bb.png"> <a href="/">Dragonball Blog</a>
				</div>
				<div class="right menu">
					<% if(!currentUser) { %>
						<a class="item" href="/login">Login</a>
						<a class="item" href="/register">Register</a>
					 <% } else {%> 
						<a class="item" href="/users/<% currentUser._id%>"><img src="https://i.pinimg.com/originals/18/3a/64/183a64ed166744236a286a2e69c916bb.png"><%= currentUser.username %></a>
						<a class="item" href="/logout">Logout</a>
					 <% } %>
				</div>
			</div>
		</div>

		<div class="ui main text container">
			<% if(error && error.length > 0) { %>
				<div class="ui red message" role="alert" id="popupMessage"><%= error %></div>
			<% } %>

			<% if(success && success.length > 0) { %>
				<div class="ui green message" role="alert"id="popupMessage"><%= success %></div>
			<% } %>
		</div>